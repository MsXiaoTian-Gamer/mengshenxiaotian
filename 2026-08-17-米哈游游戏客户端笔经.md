# 米哈游游戏客户端笔经

> 准备了满满的 C# 和 Unity 八股，结果试卷基本都是 C++ 的内容…… 大概率是凉了。
> 但还是把还记得的题目整理出来，供后来人参考。

<style>
.lc-tabs{position:relative;margin:1em 0;border-radius:8px;overflow:hidden;box-shadow:0 2px 10px rgba(0,0,0,.15);border:1px solid #333}
.lc-tabs input{display:none}
.lc-bar{display:flex;background:#1e1e1e;border-bottom:1px solid #333}
.lc-tab{padding:9px 18px;font-size:13px;color:#8a8a8a;cursor:pointer;user-select:none;border-bottom:2px solid transparent;transition:color .15s}
.lc-tab:hover{color:#d4d4d4}
.lc-panel{display:none;background:#1e1e1e;padding:14px 16px;overflow-x:auto}
.lc-panel pre{margin:0;background:transparent!important}
.lc-panel code{font-family:"SF Mono",Consolas,Menlo,monospace!important;font-size:13.5px;line-height:1.65;color:#d4d4d4!important;background:transparent!important;padding:0!important}
#lc1-py:checked ~ .lc-bar label[for="lc1-py"],#lc1-cpp:checked ~ .lc-bar label[for="lc1-cpp"],#lc1-cs:checked ~ .lc-bar label[for="lc1-cs"],
#lc2-py:checked ~ .lc-bar label[for="lc2-py"],#lc2-cpp:checked ~ .lc-bar label[for="lc2-cpp"],#lc2-cs:checked ~ .lc-bar label[for="lc2-cs"]{color:#ffa116;border-bottom-color:#ffa116}
#lc1-py:checked ~ .lc-panel.lc-py,#lc1-cpp:checked ~ .lc-panel.lc-cpp,#lc1-cs:checked ~ .lc-panel.lc-cs,
#lc2-py:checked ~ .lc-panel.lc-py,#lc2-cpp:checked ~ .lc-panel.lc-cpp,#lc2-cs:checked ~ .lc-panel.lc-cs{display:block}
</style>

---

## 一、题型概览

| 题型 | 数量 | 考察内容 |
|------|------|----------|
| 单选题 | 11 道 | C++ 基础、多线程、计算机网络 |
| 多选题 | 14 道 | C++ 基础、STL、网络协议 |
| 算法题 | 2 道 | 括号序列计数、数论 + BFS |
| vibecoding | 1 道 | 现场修一个 HTML 小游戏 |

整体感受：**笔试内容和我准备的（C# + Unity）完全错位**，C++ 占比极高，纯靠 C# 经验硬猜。如果有同学想去米哈游客户端，建议**优先复习 C++**，Unity 相关反而考得少。

---

## 二、选择题回忆

### 1. C++ 基础（占大头）

回忆到的考点：

| 考点 | 大致内容 |
|------|----------|
| 多态与继承 | 继承链中函数被 `virtual` 修饰后的调用输出顺序 |
| 虚函数 | 虚函数表、析构函数为什么需要虚函数 |
| 智能指针 | `unique_ptr` / `shared_ptr` 的用法和区别 |
| STL 容器 | `vector` 的内存分配机制（扩容策略、迭代器失效） |
| 多线程 | 死锁产生的四个必要条件 |

**踩坑点**：多态那题要特别小心"基类指针调用虚函数 vs 非虚函数"的输出顺序，以及构造/析构函数里调用虚函数**不会发生多态**（会调用当前类的版本）。这是经典考点。

### 2. 计算机网络

| 考点 | 大致内容 |
|------|----------|
| IP 分配 | 网络断开后 IP 的重新分配机制（DHCP 租约、续租、释放） |
| 延迟优化 | 网络延迟高的优化手段（TCP 优化、CDN、连接复用等） |
| ACK 重发 | 客户端反复重发消息、服务器迟迟不返回 ACK 的处理 |

**踩坑点**：DHCP 那题建议理解整个流程——租约获取、租期过半续租、租约到期释放。延迟优化是多选题重灾区，注意区分"传输层优化"和"应用层优化"的手段。

---

## 三、算法题 1：合法括号序列反转区间计数

### 题目描述

给定一个合法括号字符串 `s`（保证输入本身一定是合法括号序列），统计满足下述条件的连续区间 `[l, r]` 的数量：

1. 将子串 `s[l..r]` 反转（仅颠倒字符顺序，括号本身不改变）；
2. 反转后得到的新字符串仍然是合法括号序列。

**样例：**

| 输入 | 输出 |
|------|------|
| 3, `()` | 2 |
| 4, `(())` | 7 |
| 4, `()()` | 7 |

### 思路分析

经典的括号序列题套路：把 `(` 记为 `+1`，`)` 记为 `-1`，定义前缀和 `prefix[i]` 为前 `i` 个字符的和。

**合法括号序列**满足两个条件：
- 所有前缀和 `prefix[i] >= 0`
- 总前缀和 `prefix[n] == 0`

反转区间 `[l, r]` 后，新序列的前缀和会怎样变化？

- `i < l`：前缀和不变，仍然合法；
- `i > r`：前缀和不变（整体字符集合没变），仍然合法；
- `l <= i <= r`：新前缀和 = `prefix[l-1] + prefix[r] - prefix[i-1]`

所以只需保证区间内部所有位置的前缀和非负，即：

> `prefix[l-1] + prefix[r] >= max(prefix[l-1], ..., prefix[r-1])`

暴力枚举所有区间并检查这个条件即可，复杂度 `O(n^2)`。

### 参考代码

<div class="lc-tabs">
<input type="radio" name="lc1" id="lc1-py" checked>
<input type="radio" name="lc1" id="lc1-cpp">
<input type="radio" name="lc1" id="lc1-cs">
<nav class="lc-bar">
<label class="lc-tab" for="lc1-py">Python</label>
<label class="lc-tab" for="lc1-cpp">C++</label>
<label class="lc-tab" for="lc1-cs">C#</label>
</nav>
<section class="lc-panel lc-py"><pre><code class="language-py">def count_valid_ranges(s: str) -&gt; int:
    n = len(s)
    # 计算前缀和：'(' -&gt; +1, ')' -&gt; -1
    prefix = [0] * (n + 1)
    for i in range(1, n + 1):
        prefix[i] = prefix[i - 1] + (1 if s[i - 1] == '(' else -1)

    ans = 0
    for l in range(1, n + 1):
        # 维护区间 [l-1, r-1] 内前缀和的最大值
        mx = prefix[l - 1]
        for r in range(l, n + 1):
            mx = max(mx, prefix[r - 1])
            # 条件：prefix[l-1] + prefix[r] &gt;= 区间内最大前缀和
            if prefix[l - 1] + prefix[r] &gt;= mx:
                ans += 1
    return ans

# 验证样例
print(count_valid_ranges("()"))    # 2
print(count_valid_ranges("(())"))  # 7
print(count_valid_ranges("()()"))  # 7</code></pre></section>
<section class="lc-panel lc-cpp"><pre><code class="language-cpp">#include &lt;iostream&gt;
#include &lt;string&gt;
#include &lt;vector&gt;
#include &lt;algorithm&gt;
using namespace std;

int countValidRanges(const string&amp; s) {
    int n = (int)s.size();
    // 计算前缀和：'(' -&gt; +1, ')' -&gt; -1
    vector&lt;int&gt; prefix(n + 1, 0);
    for (int i = 1; i &lt;= n; ++i) {
        prefix[i] = prefix[i - 1] + (s[i - 1] == '(' ? 1 : -1);
    }

    int ans = 0;
    for (int l = 1; l &lt;= n; ++l) {
        // 维护区间 [l-1, r-1] 内前缀和的最大值
        int mx = prefix[l - 1];
        for (int r = l; r &lt;= n; ++r) {
            mx = max(mx, prefix[r - 1]);
            // 条件：prefix[l-1] + prefix[r] &gt;= 区间内最大前缀和
            if (prefix[l - 1] + prefix[r] &gt;= mx) ++ans;
        }
    }
    return ans;
}

int main() {
    cout &lt;&lt; countValidRanges("()") &lt;&lt; endl;    // 2
    cout &lt;&lt; countValidRanges("(())") &lt;&lt; endl;  // 7
    cout &lt;&lt; countValidRanges("()()") &lt;&lt; endl;  // 7
    return 0;
}</code></pre></section>
<section class="lc-panel lc-cs"><pre><code class="language-cs">using System;

class Program
{
    static int CountValidRanges(string s)
    {
        int n = s.Length;
        // 计算前缀和：'(' -&gt; +1, ')' -&gt; -1
        int[] prefix = new int[n + 1];
        for (int i = 1; i &lt;= n; i++)
        {
            prefix[i] = prefix[i - 1] + (s[i - 1] == '(' ? 1 : -1);
        }

        int ans = 0;
        for (int l = 1; l &lt;= n; l++)
        {
            // 维护区间 [l-1, r-1] 内前缀和的最大值
            int mx = prefix[l - 1];
            for (int r = l; r &lt;= n; r++)
            {
                mx = Math.Max(mx, prefix[r - 1]);
                // 条件：prefix[l-1] + prefix[r] &gt;= 区间内最大前缀和
                if (prefix[l - 1] + prefix[r] &gt;= mx) ans++;
            }
        }
        return ans;
    }

    static void Main()
    {
        Console.WriteLine(CountValidRanges("()"));    // 2
        Console.WriteLine(CountValidRanges("(())"));  // 7
        Console.WriteLine(CountValidRanges("()()"));  // 7
    }
}</code></pre></section>
</div>

**优化方向**：`n` 较大时可以用线段树 / 单调栈把复杂度降到 `O(n log n)`，笔试阶段写出 `O(n^2)` 的暴力验证 + 样例通过即可。

---

## 四、算法题 2：加因数走到质数

### 题目描述

给定整数 `n`，随后输入 `n` 个正整数。

一次操作：对于当前数字 `x`，选出它**任意一个正因数** `d`，执行 `x = x + d`。

目标：通过若干次操作，使数字变为**质数**，求**最少操作步数**。

**样例：**

| 输入 | 输出 |
|------|------|
| `1` | 1 |
| `3` | 0 |
| `5` | 2 |
| `14` | 2 |
| `25` | 2 |

> 解释：`5 → 6 (+1) → 7 (+1)` 两步；`14 → 28 (+14) → 29 (+1)` 两步；`25 → 30 (+5) → 31 (+1)` 两步。

### 思路分析

经典的**最短步数问题**，直接上 BFS：

- 从 `x` 出发；
- 每一步枚举当前数字的所有因数 `d`（用 `O(sqrt(x))` 枚举）；
- 转移到 `x + d`；
- 第一次到达质数时的层数就是答案。

注意点：
- `x` 本身是质数时答案是 `0`（样例 `3 → 0`）；
- 质数判定也要用 `O(sqrt(x))` 的试除法；
- 因为相邻质数间距有限，可以给 BFS 设置步数上限防止无限扩展。

### 参考代码

<div class="lc-tabs">
<input type="radio" name="lc2" id="lc2-py" checked>
<input type="radio" name="lc2" id="lc2-cpp">
<input type="radio" name="lc2" id="lc2-cs">
<nav class="lc-bar">
<label class="lc-tab" for="lc2-py">Python</label>
<label class="lc-tab" for="lc2-cpp">C++</label>
<label class="lc-tab" for="lc2-cs">C#</label>
</nav>
<section class="lc-panel lc-py"><pre><code class="language-py">import math
from collections import deque

def is_prime(x: int) -&gt; bool:
    if x &lt; 2:
        return False
    if x &lt; 4:
        return True
    if x % 2 == 0:
        return False
    i = 3
    while i * i &lt;= x:
        if x % i == 0:
            return False
        i += 2
    return True

def min_steps(x: int) -&gt; int:
    if is_prime(x):
        return 0
    q = deque([(x, 0)])
    visited = {x}
    # 安全上限：相邻质数间距有限，10 步足够覆盖常规情况
    while q:
        cur, step = q.popleft()
        if step &gt;= 10:
            continue
        # 枚举所有因数
        for d in range(1, int(math.isqrt(cur)) + 1):
            if cur % d == 0:
                for nd in (d, cur // d):
                    nxt = cur + nd
                    if nxt in visited:
                        continue
                    if is_prime(nxt):
                        return step + 1
                    visited.add(nxt)
                    q.append((nxt, step + 1))
    return -1

# 验证样例
for x in [1, 3, 5, 14, 25]:
    print(x, min_steps(x))
# 1 1
# 3 0
# 5 2
# 14 2
# 25 2</code></pre></section>
<section class="lc-panel lc-cpp"><pre><code class="language-cpp">#include &lt;iostream&gt;
#include &lt;queue&gt;
#include &lt;unordered_set&gt;
#include &lt;utility&gt;
using namespace std;

bool isPrime(int x) {
    if (x &lt; 2) return false;
    if (x &lt; 4) return true;
    if (x % 2 == 0) return false;
    for (int i = 3; i * i &lt;= x; i += 2)
        if (x % i == 0) return false;
    return true;
}

int minSteps(int x) {
    if (isPrime(x)) return 0;
    queue&lt;pair&lt;int, int&gt;&gt; q;          // {当前值, 步数}
    unordered_set&lt;int&gt; visited;
    q.push({x, 0});
    visited.insert(x);

    while (!q.empty()) {
        auto [cur, step] = q.front(); q.pop();
        if (step &gt;= 10) continue;     // 安全上限
        // 枚举所有因数
        for (int d = 1; d * d &lt;= cur; ++d) {
            if (cur % d != 0) continue;
            int ds[2] = {d, cur / d};
            for (int nd : ds) {
                int nxt = cur + nd;
                if (visited.count(nxt)) continue;
                if (isPrime(nxt)) return step + 1;
                visited.insert(nxt);
                q.push({nxt, step + 1});
            }
        }
    }
    return -1;
}

int main() {
    int nums[] = {1, 3, 5, 14, 25};
    for (int x : nums)
        cout &lt;&lt; x &lt;&lt; " " &lt;&lt; minSteps(x) &lt;&lt; endl;
    // 1 1 / 3 0 / 5 2 / 14 2 / 25 2
    return 0;
}</code></pre></section>
<section class="lc-panel lc-cs"><pre><code class="language-cs">using System;
using System.Collections.Generic;

class Program
{
    static bool IsPrime(int x)
    {
        if (x &lt; 2) return false;
        if (x &lt; 4) return true;
        if (x % 2 == 0) return false;
        for (int i = 3; i * i &lt;= x; i += 2)
            if (x % i == 0) return false;
        return true;
    }

    static int MinSteps(int x)
    {
        if (IsPrime(x)) return 0;
        var q = new Queue&lt;(int val, int step)&gt;();   // {当前值, 步数}
        var visited = new HashSet&lt;int&gt;();
        q.Enqueue((x, 0));
        visited.Add(x);

        while (q.Count &gt; 0)
        {
            var (cur, step) = q.Dequeue();
            if (step &gt;= 10) continue;               // 安全上限
            // 枚举所有因数
            for (int d = 1; d * d &lt;= cur; d++)
            {
                if (cur % d != 0) continue;
                int[] ds = { d, cur / d };
                foreach (int nd in ds)
                {
                    int nxt = cur + nd;
                    if (visited.Contains(nxt)) continue;
                    if (IsPrime(nxt)) return step + 1;
                    visited.Add(nxt);
                    q.Enqueue((nxt, step + 1));
                }
            }
        }
        return -1;
    }

    static void Main()
    {
        int[] nums = { 1, 3, 5, 14, 25 };
        foreach (int x in nums)
            Console.WriteLine(x + " " + MinSteps(x));
        // 1 1 / 3 0 / 5 2 / 14 2 / 25 2
    }
}</code></pre></section>
</div>

---

## 五、vibecoding 题

最后一道是现场修一个 HTML 小游戏。模型生成的代码比较粗糙，**连 CSS 都能写错**（我前端不熟都能一眼找到错误）。

考察点其实很明确：
- 快速定位语法错误（HTML / CSS / JS）；
- 修复逻辑 bug 后让游戏能跑起来；
- 不要求多优雅，**能跑就行**。

建议平时多写写简单的小游戏（比如贪吃蛇、打砖块），面试时至少不慌。

---

## 六、总结与反思

1. **复习方向错了**：米哈游客户端笔试 C++ 占比极高，我准备的 C# + Unity 八股几乎没用上。建议想去米哈游的同学**重点攻 C++**（虚函数、智能指针、STL、多线程），Unity 反而考得少。
2. **算法题难度中等**：两道题都是经典题型（括号序列 + BFS 最短路），平时 LeetCode 刷过类似题基本能写出来。
3. **网络基础不能丢**：选择题里网络占了不小比例，DHCP、TCP、延迟优化这些基础要过一遍。
4. **心态放平**：考完大概率是凉的，但也算摸清了方向。下次再战！

> 祝看到这篇笔经的同学好运，希望你们不用踩我踩过的坑。
